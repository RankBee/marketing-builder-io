# Vite client-side environment (DO NOT PUT SECRET KEYS HERE)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx_or_pk_live_xxx

# Dashboard URL used for “View Your Dashboard” CTA
VITE_APP_URL=

# Where to send users after sign-up to complete setup
# Can be absolute (https://rankbee.ai/onboard) or relative (/onboard)
VITE_ONBOARD_URL=

# Canonical base URL for SEO tags (used client-side by react-helmet-async)
# In dev you can leave this as http://localhost:3000
VITE_SITE_URL=https://rankbee.ai

# App environment for analytics/intercom logic:
# production → use VITE_GTM_ID_PROD; Intercom enabled
# preprod    → use VITE_GTM_ID_STG;  Intercom enabled
# anything else (staging, development) → use VITE_GTM_ID_STG; Intercom disabled
VITE_APP_ENV=development

# Google Tag Manager
VITE_GTM_ID_STG=GTM-XXXXSTG
VITE_GTM_ID_PROD=GTM-XXXXPROD

# PostHog (optional; can also be injected via GTM)
VITE_POSTHOG_KEY=phc_your_public_key
VITE_POSTHOG_HOST=https://app.posthog.com

# Intercom (optional)
VITE_INTERCOM_APP_ID=abc12345

# Note: CLERK_SECRET_KEY and any server secrets must NOT be present in this Vite app

# Netlify Functions read SITE_URL server-side (unprefixed)
# Set in Netlify dashboard → Site settings → Build & deploy → Environment → Environment variables
# SITE_URL should match your production canonical (not the preview URL)
# SITE_URL=https://rankbee.ai