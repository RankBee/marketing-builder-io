# Vite client-side environment (DO NOT PUT SECRET KEYS HERE)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx_or_pk_live_xxx

# Sign In and Sign Up URLs (for cross-app redirect)
VITE_SIGN_IN_URL=https://onboard.rankbee.ai/sign-in
VITE_SIGN_UP_URL=https://onboard.rankbee.ai/register

# Dashboard URL used for “View Your Dashboard” CTA
VITE_APP_URL=

# Where to send users after sign-up to complete setup
# Can be absolute (https://rankbee.ai/onboard) or relative (/onboard)
VITE_ONBOARD_URL=

# Canonical base URL for SEO tags and build-time SEO generation
# Used by react-helmet-async AND scripts/generate-seo.mjs (robots.txt, sitemap.xml)
# In dev you can leave this as http://localhost:3000
VITE_SITE_URL=https://rankbee.ai

# App environment for analytics/intercom logic:
# production → use VITE_GTM_ID_PROD; Intercom enabled
# preprod    → use VITE_GTM_ID_STG;  Intercom enabled
# anything else (staging, development) → use VITE_GTM_ID_STG; Intercom disabled
VITE_APP_ENV=development

# Google Tag Manager
VITE_GTM_ID_STG=GTM-XXXXSTG
VITE_GTM_ID_PROD=GTM-XXXXPROD

# PostHog (optional; can also be injected via GTM)
VITE_POSTHOG_KEY=phc_your_public_key
VITE_POSTHOG_HOST=https://app.posthog.com

# Intercom (optional)
VITE_INTERCOM_APP_ID=abc12345

# Calendly Embed URLs (optional overrides)
# Used for /demo and /onboarding-meeting
# Logic: VITE_CALENDLY_EU for EU timezones, VITE_CALENDLY_OTHERS for ROW
VITE_CALENDLY_EU=
VITE_CALENDLY_OTHERS=

# Build-time SEO generator fallbacks and server-side notes
# - SITE_URL: optional alternative to VITE_SITE_URL (used by scripts/generate-seo.mjs)
# - APP_ENV: optional alternative to VITE_APP_ENV; 'production' or 'prod' allows indexing; anything else disallows
# - CONTEXT: Netlify only (production | deploy-preview | branch-deploy | dev); ignored on DigitalOcean
# Example:
# SITE_URL=https://rankbee.ai
# APP_ENV=production
#
# Netlify Functions read SITE_URL server-side (unprefixed)
# Set in Netlify dashboard → Site settings → Build & deploy → Environment → Environment variables
# SITE_URL should match your production canonical (not the preview URL)
# SITE_URL=https://rankbee.ai

# PostHog Analytics
VITE_POSTHOG_KEY=phc_xxx
VITE_POSTHOG_HOST=https://app.posthog.com

# App Environment (production, preprod, development)
VITE_APP_ENV=development

# Google Tag Manager
VITE_GTM_ID=GTM-XXXXXXX

# Builder.io Content API
VITE_BUILDER_API_KEY=4d7724b1d3ab0bbf970850bf7f
VITE_BUILDER_API_URL=https://cdn.builder.io