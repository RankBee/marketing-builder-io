# Vite client-side environment for Clerk (DO NOT PUT SECRET KEYS HERE)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx_or_pk_live_xxx

# Dashboard URL used for "View Your Dashboard" CTA
VITE_APP_URL=https://app.rankbee.ai

# Where to send users for onboarding when "Complete Setup" is shown.
# Can be absolute (e.g., https://rankbee.ai/onboard) or relative (/onboard) depending on deployment routing.
VITE_ONBOARD_URL=https://rankbee.ai/onboard

# Optional (not required now that Sign In is in this app).
# Keep only if you need to force a different sign-in location during local dev.
# VITE_SIGN_IN_URL=http://localhost:3001/sign-in

# Note: CLERK_SECRET_KEY is server-side only and must NOT be present in this Vite app

# Canonical base URL for SEO tags (used client-side by react-helmet-async)
VITE_SITE_URL=https://rankbee.ai

# Netlify Functions read this server-side (not prefixed with VITE_)
# Set in Netlify dashboard → Site settings → Build & deploy → Environment → Environment variables
# SITE_URL should match your production canonical (not the preview URL)
# SITE_URL=https://rankbee.ai